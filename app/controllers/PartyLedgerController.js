const accountObj = require('../models/Account');
const amadObj = require('../models/Amad');
exports.getprtyLedger = async (req,res)=>{
try {
    const amad = amadObj.findOne({})
} catch (error) {
    
}

}